# Kira Visual - Minimal config for bash visual mode
# Separate from Tom's main kitty.conf (which may use nvim as shell)

allow_remote_control yes
confirm_os_window_close 0

# Match Tom's appearance
background_opacity 1
font_family SourceCodePro-Medium
font_size 16.0
