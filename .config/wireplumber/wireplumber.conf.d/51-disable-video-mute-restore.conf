# Disable automatic mute restoration for video streams
# This prevents WirePlumber from auto-muting MKV/video playback

stream.rules = [
  {
    matches = [
      {
        media.role = "video"
      }
    ]
    actions = {
      update-props = {
        state.restore-props = false
      }
    }
  }
]
